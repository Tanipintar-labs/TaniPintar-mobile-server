services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./certs:/app/certs:ro
    restart: unless-stopped
